# Run with Docker

Build the image and start the dev server:

```bash
docker compose build
docker compose up
```

The app will be available at http://localhost:3000

Notes:
- Mounting the project directory enables hot reload.
- If you prefer running locally, install Node.js and run `npm install` then `npm run dev`.
